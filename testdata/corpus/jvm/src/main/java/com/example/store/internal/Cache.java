// Corpus fixture: not compiled, not run.
package com.example.store.internal;

/** A cache nobody outside the store package should hold. */
public class Cache {
    /** Empties the cache. */
    public void clear() {}
}
