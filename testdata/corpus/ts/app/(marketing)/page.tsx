// A Next.js route group. Parentheses are what terminate a markdown link target,
// so this covers the emitter's link escaping rather than its YAML quoting.
import React from "react";

export default function MarketingPage() {
  return <p>marketing</p>;
}
