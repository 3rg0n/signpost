[project]
name = "corpus-greeter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.28", "opentelemetry-sdk>=1.29"]

[project.optional-dependencies]
dev = ["pytest>=8.3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
