---
// Corpus fixture: not compiled, not run. Two files rather than one, so the count
// asserted in TestCorpusCountsWhatItCannotClassify cannot be satisfied by an
// implementation that reports the extension once however many files carry it.
import Base from "../layouts/Base.astro";
---
<Base />
